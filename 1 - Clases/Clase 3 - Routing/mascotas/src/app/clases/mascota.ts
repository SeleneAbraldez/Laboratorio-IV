export class Mascota {
    nombre: string;
    foto: string;
    tipo: string;
    id: number;

    constructor(nombre, img, tipo, id){
        this.nombre = (nombre);
        this.foto = (img);
        this.tipo = (tipo);
        this.id = (id);
    }
}

