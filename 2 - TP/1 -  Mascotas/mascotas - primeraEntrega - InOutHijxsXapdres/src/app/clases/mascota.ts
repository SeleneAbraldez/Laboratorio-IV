export class Mascota {
    nombre: string;
    foto: string;
    tipo: string;

    constructor(nombre, tipo, img){
        this.nombre = (nombre);
        this.foto = (img);
        this.tipo = (tipo);
    }
}

